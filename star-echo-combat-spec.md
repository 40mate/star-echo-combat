# 별의 잔향 — 전투 시스템 기획서

**전제:** 진형 기반 커맨드 턴제 RPG. 완전 수동 전투(자동 전투 없음, 배속 지원, 스킵 없음). 타일 이동·지형·사거리 개념 없음.

---

## 용어

| 용어 | 정의 |
|---|---|
| 유닛(Unit) | 아군 캐릭터와 몬스터의 상위 개념. 전투에 참여하는 모든 개체 |
| 효과(Effect) | 스킬을 구성하는 최소 기능 단위. 데미지·회복·버프·도발이 전부 효과의 한 종류. 모든 스킬은 효과의 조합으로 정의하며, 효과 타입 하나는 코드상 핸들러 하나에 1:1 대응한다 |
| 연출(VFX) | 파티클·사운드·카메라 등 시각/청각 표현. 효과(로직)와 완전히 별개 레이어 |
| effective 스탯 | 버프/디버프가 모두 반영된 실시간 유효값. base 스탯과 구분 |
| 자기 턴 | 해당 유닛이 행동 주체가 되는 턴. 쿨타임·지속 턴 카운트의 기준 |
| 주 대상 | 스킬 시전 시 선택되는 대상. 스킬당 1회 선택하며, 효과들이 공유한다 |
| 전투 | 챕터 진입부터 클리어/패배까지의 연속 웨이브 전체. RNG 시드·이벤트 순번의 단위 |
| `타입?` 표기 | 스키마 표에서 타입 뒤의 `?`는 선택(optional) 필드 — 값이 없어도 되는 필드. `?`가 없으면 필수. `[]`는 배열, enum은 열거형 |

---

## 구현 순서

구현 순서는 Phase 번호와 동일한 단위로 관리한다. 앞 단계가 뒤 단계의 전제다.

| 순서 | Phase | 구현 대상 | 비고 |
|---|---|---|---|
| 1 | Phase 1 | RNG·이벤트 버스 | 재현·로그·반응형 연출의 토대 |
| 2 | Phase 2 | 데이터 스키마 | 런타임 순수 데이터로 변환해 사용 |
| 3 | Phase 3 | 전투 상태·그리드 | |
| 4 | Phase 4 | 턴 엔진 | |
| 5 | Phase 5·6·7 | 타겟팅 · 스킬 선택 · 효과 정산 | 서로 병렬 구현 가능 |
| 6 | Phase 8·10 | 몬스터 AI · 종료 판정 | 정산 결과 훅 위에서 동작 |
| 7 | Phase 9 | 웨이브·챕터 진행 | 전투 바깥 루프 |
| 8 | Phase 11 | 시뮬레이터 | Phase 12보다 먼저(밸런싱 루프 확보) |
| 9 | Phase 12 | UI/UX | 로직 완성 위에 렌더만 |

---

# Phase 1 — 기반 런타임

전투에서 일어나는 모든 일을 "같은 입력이면 같은 결과"로 만들기 위한 토대다. 확률 판정을 한 줄기로 모으고 모든 사건을 하나의 흐름으로 기록해, 리플레이·버그 재현·밸런싱 시뮬레이션·연출이 전부 같은 데이터에서 파생되게 한다.

## 1. RNG 시드 스트림

전투의 모든 확률 판정은 단일 시드 RNG 스트림에서만 뽑는다.

| 필드 | 타입 | 설명 |
|---|---|---|
| seed | uint64 | 챕터 진입(전투 시작) 시 1회 고정. 웨이브 전환에도 스트림 연속 |
| stream | — | PCG/xorshift 등 결정론적 시퀀스 |
| next() | float | [0, 1) 반환 |

- **재현성:** 동일 (편성 + 배치 + 적 덱 + seed) → 완전히 동일한 전투 결과. 시뮬레이터·버그 재현·밸런싱의 전제.
- **호출 규율:** RNG 호출 지점·횟수·순서가 흔들리면 재현성이 깨진다. 호출은 아래로 국한한다.
  - 치명타 판정: 효과 정산 순서(Phase 7)에 따라 대상별 정확히 1회.
  - AI 타겟 추첨: 타겟 확정 단계에서 정확히 1회. 후보 열거 순서는 슬롯 순(F1→F2→F3→B1→B2→B3) 고정, 추첨값을 누적 확률 구간에 매핑.
  - 타겟 후보 집합 산출은 항상 순수 함수이며 RNG를 사용하지 않는다. RNG는 최종 확정된 스킬의 타겟 확정 단계에서만 소비한다.
- **광역 처리 순서:** 광역 효과의 대상 열거·정산·RNG 소비는 슬롯 순(F1→F2→F3→B1→B2→B3) 고정.
- **RNG를 소비하지 않는 경우:** 확정 치명타(guaranteedCrit), 후보가 1명뿐인 선정, HP 최저 등 결정론적 선정, 도발 강제.
- **시드 소스:** 실플레이는 시각·유닛 구성 해시 등에서 파생(패배 후 재시작 시 새로 파생). 시뮬레이터는 명시적 seed 주입.

## 2. 전투 이벤트 버스 & 로그

전투 중 발생하는 모든 사건은 단일 이벤트 스트림으로 방출된다. 반응형 연출·전투 로그·기여도 분석·디버그가 전부 이 스트림을 구독만 한다. 로직은 이벤트를 방출할 뿐 구독자를 모른다(단방향).

**CombatEvent**

| 필드 | 타입 | 설명 |
|---|---|---|
| seq | int | 전역 순번. 정산 순서에 따라 단조 증가, 전투(챕터) 전체에서 연속 |
| round | int | 현재 라운드(웨이브 내부 기준) |
| type | EventType | 아래 표 |
| source | unitRef? | 시전자 |
| target | unitRef? | 대상 |
| effectId | string? | 유발 효과 id |
| value | float? | 피해/회복/증감량 등 |
| meta | object? | 부가 정보 |

**EventType**

| 분류 | 값 |
|---|---|
| 전투 경계 | BATTLE_START, BATTLE_END, WAVE_START, WAVE_END, ROUND_START, ROUND_END |
| 턴 | TURN_START, TURN_END, TURN_SKIPPED |
| 스킬·효과 | SKILL_USED, EFFECT_APPLIED |
| 피해·회복 | HIT, CRIT, HEAL, DOT_TICK, LIFESTEAL |
| 상태 | STAT_MODIFIER_ADDED, STAT_MODIFIER_EXPIRED, DOT_ADDED, DOT_PENDING, IMMUNITY_BLOCK, TAUNT_APPLIED, REDUCTION_APPLIED |
| 기타 | DEATH, SPEED_REORDER |

- 같은 seed → 같은 이벤트 시퀀스. 로그 = 이벤트 스트림의 직렬화(별도 로그 코드 없음).
- 이벤트는 순수 데이터로 Unity 비의존 → 시뮬레이터에서 그대로 수집.
- 구독 매핑은 위 정식 이벤트명 문자열을 그대로 사용한다(축약·별칭 금지).

---

# Phase 2 — 데이터 스키마

스킬을 코드가 아니라 데이터로 만들기 위한 틀이다. 24챕터 분량의 캐릭터·몬스터를 기획자가 직접 조립할 수 있어야 콘텐츠 생산 속도가 유지된다. 이를 위해 데미지·회복·버프·도트·도발·보호는 전부 "효과의 종류"로 환원하고, 캐릭터 추가는 데이터 작업만으로 이뤄지게 한다.

## 1. 스탯

공식 스탯 7종. 정수 저장을 원칙으로 하되, 확률·피해 계수는 소수를 허용한다. 회피·명중 스탯 없음 — 모든 공격은 명중하며, 확률 판정은 치명타 하나로 국한한다. 운의 개입 지점을 하나로 좁힌 것은 의도다: 확률 요소가 하나여야 플레이어가 결과를 운이 아니라 계획의 성패로 받아들인다.

**StatBlock**

| 필드 | 타입 | 설명 |
|---|---|---|
| atk | int | 공격력 |
| def | int | 방어력 |
| hp | int | 최대 체력(현재 HP는 런타임 상태) |
| critRate | float | 치명타 확률(0.0~1.0). 게임 내 유일한 확률 판정 |
| critDmg | float | 치명타 피해(배율 가산분, 예: 0.5 = +50%) |
| critRes | float | 치명타 저항. 확률 차감이 아니라 치명 피해량 상쇄 |
| spd | int | 행동 속도. 턴 순서 결정자 |

**버프/디버프 합산 — 계산 그룹**

합산 방식은 전역 고정이 아니라 STAT_MODIFIER 효과 데이터의 `meta.calcGroup`에 지정한다.

| calcGroup | 의미 | 합산 |
|---|---|---|
| ADD_FLAT | 고정값 가감 | 같은 스탯의 값들 단순 합산 |
| ADD_PERCENT | 가산 퍼센트 | 값들을 더한 뒤 1회 곱 |
| MUL | 독립 곱연산 | 인스턴스마다 개별 곱(별도 승수) |

적용 순서: `effective = (base + Σ ADD_FLAT) × (1 + Σ ADD_PERCENT) × Π(1 + MUL_i)`

- 예) base atk 120에 +30%(ADD_PERCENT) → 156. 별도 MUL +20%가 붙으면 156 × 1.20 = 187.
- 합산은 같은 스탯 키 안에서만 이뤄진다. 피해 감쇄(보호)의 곱연산은 이 파이프라인과 별개다.

**스탯 클램프** — effective 산출의 마지막 단계에 1회 적용. base 값은 변경하지 않는다.

| 대상 | 범위 |
|---|---|
| critRate | 0 ~ 1 |
| critDmg, critRes, atk, def, elementDef | 하한 0 |
| spd | 하한 1 (0이면 영원히 행동 불가 방지) |
| hp(최대 체력) | 하한 1 |

**최대 체력 증감 시 현재 HP 처리**
- 최대 체력 증가: 현재 HP 유지(회복 없음). HP 비율만 하락.
- 최대 체력 감소·버프 만료: `currentHp = min(currentHp, 새 최대 체력)`.

## 2. 속성

| 항목 | 정의 |
|---|---|
| Element | FIRE, WATER, GRASS, LIGHT, DARK |
| 상성 | 없음(상성 삼각형 없음) |
| 속성 추가 배율 | 스킬에 부여된 속성당 +0.3 (2속성이면 각 +0.3) |
| 속성 방어(elementDef) | 유닛이 속성별로 가지는 피해 상쇄값. 속성별 맵으로 관리, 기본 0 |

속성 간 유불리는 "추가 배율 − 속성 방어"의 상호작용으로만 발생한다. **속성 이점 = 속성 방어 디버프.** 예를 들어 "빛·풀이 어둠에 유리"는 대상의 어둠 속성 방어를 낮추는 `STAT_MODIFIER(ELEMENT_DEF_DARK, down)` 효과를 부여해 구현한다. 밸런싱은 스킬 데이터로 통제된다.

**StatKey** — STAT_MODIFIER가 조작할 수 있는 스탯 키. 신규 스탯은 여기에 등재해야 조작 가능.

| 분류 | 키 |
|---|---|
| 기본 7스탯 | ATK, DEF, HP, CRIT_RATE, CRIT_DMG, CRIT_RES, SPD |
| 속성 방어 | ELEMENT_DEF_FIRE, ELEMENT_DEF_WATER, ELEMENT_DEF_GRASS, ELEMENT_DEF_LIGHT, ELEMENT_DEF_DARK |

## 3. 클래스

| 항목 | 정의 |
|---|---|
| Class | DEFENSE, MELEE_DPS, RANGED_DPS, SUPPORT |

클래스는 전투 계산에 직접 개입하지 않는다. 용도는 UI 표기·필터링과 기획 규율(클래스에 맞는 스킬 디자인 컨벤션)뿐.

## 4. 유닛 데이터

캐릭터와 몬스터를 하나의 스키마로 통합한다. 차이는 `faction`, 스킬 슬롯 수, 정보 공개 범위뿐.

**UnitDef**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 고유 ID. 턴 순서 동률 시 최종 기준 |
| name | string | 이름 |
| faction | enum | ALLY, ENEMY |
| monsterTier | enum? | NORMAL, ELITE, BOSS (ENEMY 전용) |
| class | Class | 클래스 |
| element | Element | 캐릭터 고유 속성(스킬 속성과 별개, 표기·컨셉용) |
| level | int | 턴 순서 동률 판정용 |
| baseStats | StatBlock | 기본 스탯 |
| baseElementDef | 속성별 맵 | 기본 속성 방어 |
| skills.normal | SkillDef | 일반 공격. 필수, 항상 ACTIVE·쿨 0·발동 조건 없음 |
| skills.skill1 | SkillDef? | 아군 캐릭터는 필수, 몬스터는 선택 |
| skills.skill2 | SkillDef? | 아군 캐릭터는 필수, 몬스터는 선택 |

**전투력(power)** — 턴 순서 동률 판정에 쓰는 종합 지표. base 스탯 기준(버프 미반영, 전투 중 불변).

```
power = atk×1.0 + def×0.8 + hp×0.1 + critRate×200 + critDmg×100 + critRes×100 + spd×1.0
```

가중치는 임시값(밸런싱 단계에서 조정). 순수 정렬 비교용이라 절대값의 의미는 없고 대소만 쓴다. 최종 결정자는 ID라 실무 영향은 미미하다.

## 5. 스킬 데이터

스킬 = 효과의 조합. 스킬 자체는 발동 조건·쿨타임·타겟 범위를 관리하는 껍데기이며, 실제 작용은 전부 `effects` 배열이 담당한다.

**SkillDef**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 고유 ID |
| name | string | 이름 |
| slot | enum | NORMAL, SKILL1, SKILL2 |
| type | enum | ACTIVE, PASSIVE |
| elements | Element[] | 0~2개 |
| cooldown | int | 사용 후 자기 턴 N회 경과 시 재사용(ACTIVE 전용, PASSIVE는 0 강제) |
| initialCooldown | int | 전투 시작 시 잔여 쿨로 세팅할 값(ACTIVE 전용) |
| trigger | Trigger? | PASSIVE의 발동 트리거, 또는 ACTIVE의 발동 조건 |
| targeting | Targeting | 주 대상 선정 규칙(아래) |
| effects | Effect[] | 발동 시 실행할 효과 목록 |
| timeline | SkillTimeline? | 효과 발동 타이밍(ACTIVE 필수, PASSIVE 생략) |

**Targeting**

| 필드 | 타입 | 설명 |
|---|---|---|
| scope | TargetScope | 주 대상 선정 범위. 플레이어/AI가 이 범위에서 1회 선택(Phase 5) |
| aiPolicy | AiPolicy? | 몬스터 전용 타겟 선정 정책 오버라이드(Phase 8). 미지정 시 THREAT_WEIGHTED |
| ignoresTaunt | bool | true면 이 스킬의 단일 공격이 도발 강제를 무시한다(저격·처형 컨셉 스킬용). 기본 false |

- 타겟 선택은 스킬당 1회. 선택된 대상이 "주 대상"이 되고, `target = MAIN_TARGET`인 효과들이 이를 공유한다. 상세 규칙은 Phase 5.
- 스킬 선택: 아군은 자기 턴에 발동 가능한 스킬 중 플레이어가 직접 선택하고, 몬스터는 스킬2 → 스킬1 → 일반 공격 우선순위로 자동 선택한다(Phase 6).

## 6. 효과(Effect) 데이터

모든 스킬 작용의 최소 단위. 효과 타입은 코드상 단일 핸들러에 1:1 대응하며(효과 레지스트리), 신규 스킬은 기존 효과의 조합·파라미터 조정만으로 만든다.

**Effect 공통 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 효과 ID. 스킬 내 유일. 타임라인·로그에서 이 id로 참조 |
| type | EffectType | 아래 표 |
| target | TargetScope | 이 효과가 향하는 대상(Phase 5) |
| element | Element? | 피해/도트의 속성(없으면 무속성) |
| value | float | 계수·수치. 항상 양수(크기). 증감 방향은 meta.direction이 결정 |
| duration | int? | 지속 턴(즉발 효과는 없음) |
| durationBasis | enum? | 지속 턴 카운트 기준(아래) |
| stack | StackRule? | 중첩 규칙(버프/디버프/도트) |
| meta | object? | 타입별 부가 파라미터 |

**durationBasis** — 한 스킬 안에서도 효과마다 기준이 다를 수 있다.

| 값 | 의미 |
|---|---|
| CASTER_TURN | 시전자의 자기 턴 N회로 카운트 |
| VICTIM_TURN | 걸린 대상 각자의 자기 턴 N회로 카운트. 광역으로 걸면 대상마다 해제 시점이 다르다 |
| ROUND | 라운드 경과로 카운트 |
| REALTIME | 지속 턴 개념 없음. 조건 충족 동안만 유지, 벗어나면 즉시 해제(HP·위치 조건 패시브) |

**EffectType**

| 타입 | 설명 | 주요 meta |
|---|---|---|
| DAMAGE | 즉발 피해. value = 공격력 계수 | guaranteedCrit?, lifestealRatio? |
| HEAL | 회복. value = 시전자 공격력 계수. 초과 회복은 버림 | |
| DOT | 지속 피해. value = 계수. duration·basis 필수 | |
| STAT_MODIFIER | 스탯 증감 | stat(StatKey), calcGroup, direction(up/down) |
| DAMAGE_REDUCTION | 피해 감쇄(보호). value = 감쇄율. 곱연산 | |
| TAUNT | 타겟 강제. 단일 대상 공격에만 작용 | |
| IMMUNITY | 지정 종류의 해로운 효과 차단 | category(아래) |

**타입별 허용 TargetScope** — 위반 시 로드 에러.

| 타입 | 허용 스코프 |
|---|---|
| DAMAGE, DOT, TAUNT | 적 스코프(ENEMY_*) 또는 MAIN_TARGET(적 대상 스킬) |
| HEAL, STAT_MODIFIER(up), DAMAGE_REDUCTION, IMMUNITY | 아군 스코프(ALLY_*, SELF) 또는 MAIN_TARGET(아군 대상 스킬) |
| STAT_MODIFIER(down) | 적 스코프(ENEMY_*) 또는 MAIN_TARGET(적 대상 스킬) |

**Trigger** — 패시브 발동 및 액티브 발동 조건.

| 필드 | 타입 | 설명 |
|---|---|---|
| type | enum | 아래 표 |
| meta | object | 타입별 조건 파라미터 |

| 트리거 타입 | meta | 설명 |
|---|---|---|
| HP_THRESHOLD | who(self/target), comparator(≤/≥), ratio | HP 비율 조건. REALTIME |
| POSITION | who(self), row(FRONT/BACK) | 위치 조건. REALTIME |
| ROUND_START | — | 매 라운드 시작 |
| ON_ATTACK_LANDED | — | 이 유닛의 공격이 명중한 직후 |
| ON_CRIT_LANDED | — | 이 유닛의 공격이 치명타로 명중한 직후 |
| ON_KILL | — | 이 유닛의 공격으로 대상이 사망한 직후 |
| BATTLE_START | — | 전투(챕터 진입) 시작 시 1회 |

같은 트리거가 여러 유닛에서 동시에 성립하면 행동 속도 정렬 순(Phase 4)으로 평가한다(결정론 보장). ON_CRIT_LANDED·ON_KILL은 현 스킬 풀에 사용 예가 없는 예약 트리거다.

**시전자 사망 시 기부여 효과 존속** — 원칙: 시전자의 생존·위치가 실시간 조건인 효과(REALTIME)는 시전자가 사라지면 함께 해제되고, 부여 시점에 값이 확정되는 효과(도트·스탯 증감 등)는 시전자가 죽어도 남은 지속 동안 유지된다.

| 효과 | 시전자 사망 후 |
|---|---|
| DOT | 유지(부여 시점에 계수 확정) |
| STAT_MODIFIER | 유지(남은 지속 동안) |
| IMMUNITY | 유지 |
| DAMAGE_REDUCTION(위치 보호) | 즉시 소멸(위치·주체 생존이 REALTIME 조건) |
| TAUNT | 즉시 소멸(도발자 부재 시 강제 대상 없음) |

## 7. 중첩 규칙

같은 효과가 이미 걸려 있을 때 신규를 어떻게 처리할지를 효과 데이터의 `stack`으로 지정한다.

**StackRule**

| 필드 | 타입 | 설명 |
|---|---|---|
| rule | enum | IGNORE, SEVERAL, OVERRIDE, STACK |
| groupKey | string? | 동일 효과 판정 키. 미지정 시 자동 파생(아래) |
| maxStack | int? | rule = STACK 전용. 최대 스택(1 이상) |

**동일 효과 판정(그룹 키)**
- 자동 파생 = `type + 구분 필드`. STAT_MODIFIER는 `stat + calcGroup + direction`.
- 기본적으로 시전자는 키에 포함하지 않는다(누가 걸든 같은 효과). 시전자별 독립이 필요하면 groupKey에 시전자를 포함한다.
- **예외 — TAUNT·DAMAGE_REDUCTION은 기본 키에 시전자를 포함하고 rule = OVERRIDE.** 시전자가 다르면 공존(다중 도발, 감쇄 곱연산 성립), 같은 시전자가 다시 걸면 갱신.
- 중첩 규칙은 같은 그룹 키를 가진 효과들 사이에서만 적용된다.

**4종 규칙**

| rule | 동작 | 용도 |
|---|---|---|
| IGNORE | 이미 있으면 신규를 적용하지 않음. 지속·수치 아무것도 갱신 안 함 | 고유 패시브, 중복 금지 상시 효과 |
| SEVERAL | 별도 인스턴스로 추가. 각자 지속시간·만료 독립. 총 효과 = 인스턴스 합산 | 독립 누적 도트, 개별 표식 |
| OVERRIDE | 기존을 제거하고 새 효과로 교체. 새 수치·지속으로 재시작 | 최신만 유효한 버프/디버프 |
| STACK | 새 인스턴스 없이 스택 +1(maxStack에서 멈춤). 스택 시 지속 재시작. 수치 = value × 현재 스택 | 누적 강화, 출혈/화상 중첩 |

예 — "공격력 +10"이 두 번 들어올 때:

| rule | 결과 |
|---|---|
| IGNORE | +10 하나. 두 번째 무시 |
| SEVERAL | +10 인스턴스 2개 공존 → 합산 +20, 각자 만료 |
| OVERRIDE | 두 번째가 기존을 교체 → 값·지속 새로 시작 |
| STACK(max 3) | 스택 2 = +20, 지속 재시작. 3회째 +30, 4회째는 max 유지 |

즉발 효과(DAMAGE, HEAL)에 stack은 무의미.

## 8. 면역 카테고리

IMMUNITY의 `meta.category` 값 체계.

| category | 차단 대상 |
|---|---|
| FIRE, WATER, GRASS, LIGHT, DARK | 해당 속성이 붙은 해로운 지속 효과(DOT, STAT_MODIFIER(down), TAUNT) |
| DOT | 모든 도트 |
| DEBUFF | 모든 STAT_MODIFIER(down) |
| TAUNT | 도발 |

- **신규 차단:** 면역 지속 중, category에 해당하는 해로운 효과의 신규 부여를 무효화한다(IMMUNITY_BLOCK 이벤트).
- **기존 발동 차단:** "발동"이 있는 효과는 도트 틱뿐이다. 면역 지속 중 category 일치 도트의 틱을 무효화한다(잔여 지속 감소는 정상 진행). STAT_MODIFIER는 상시형이라 기부여분은 차단하지 않는다.
- **즉발 DAMAGE·HEAL은 면역과 무관.** 속성 면역도 해당 속성의 즉발 피해는 통과시킨다.
- 예) 어둠 면역(1턴) → 그 턴 동안 어둠 도트·어둠 디버프의 신규 부여와 기존 어둠 도트의 틱이 무효. 어둠 즉발 피해는 그대로 받는다.

## 9. 데이터 검증 규칙

빌드/로드 시점에 강제한다. 하나라도 위반이면 로드 실패.

- `normal` 슬롯은 항상 ACTIVE, cooldown 0, trigger 없음.
- `faction = ALLY`면 skill1·skill2 필수. ENEMY는 normal만 필수.
- skill1과 skill2가 둘 다 PASSIVE일 수 없다(존재하는 슬롯 기준).
- `elements` 배열 길이 ≤ 2.
- PASSIVE는 cooldown·initialCooldown 무시(0 강제), trigger 필수, timeline 생략.
- ACTIVE에 trigger가 있으면 발동 조건으로만 해석(예: HP 조건 만족 시에만 사용 가능).
- `initialCooldown ≤ cooldown` (ACTIVE 전용).
- 효과의 `target`은 타입별 허용 스코프 표를 지켜야 한다.
- `target = MAIN_TARGET`인 효과의 대상 진영은 `targeting.scope`의 진영과 일치해야 한다.
- `targeting.scope`에 MAIN_TARGET·SELF는 사용할 수 없다.
- STAT_MODIFIER는 `meta.stat`이 StatKey 값이어야 하고, `value > 0`, `meta.direction` 필수.
- `rule = STACK`이면 `maxStack ≥ 1` 필수. 그 외 규칙은 maxStack 무시.
- 모든 효과의 `id`는 스킬 내에서 유일해야 한다.
- ACTIVE 스킬의 모든 효과는 최소 1개의 타임라인 키에 바인딩되어야 한다.

## 10. 스킬 타임라인

스킬의 로직(무엇을)과 연출 타이밍(언제)을 분리한다. `effects`는 실행할 내용의 풀이고, 타임라인은 각 효과를 재생 중 어느 시점에 터뜨리는지 정의한다. 애니메이터가 키 위치만 옮겨도 타이밍이 바뀌고 로직 데이터는 그대로다.

**핵심 원칙**
- 전투 결과(데미지·회복·사망·승패)는 연출 타이밍과 무관하게 결정론적으로 산출된다. 타임라인은 결과 값을 바꾸지 않으며, 효과 간 실행 순서와 화면 노출 시점만 규정한다.
- 로직 발동을 애니메이션 이벤트(노티파이)로 하지 않는다. 애니메이션 이벤트는 발소리·잔상 등 결과와 무관한 보조 연출 전용.
- 저장 단위는 정규화 시간 0.0~1.0(클립 길이·프레임레이트 변화에 강건). 에디터 표기용 프레임은 `at × duration × fps`로 역산.

**SkillTimeline**

| 필드 | 타입 | 설명 |
|---|---|---|
| clipId | string | 애니메이션 클립 참조 |
| duration | float | 클립 총 길이(초) |
| keys | TimelineKey[] | 발동 키 목록 |

**TimelineKey**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 키 ID |
| at | float | 정규화 시간 0.0~1.0 |
| effectIds | string[] | 이 시점에 발동할 효과 id 목록 |

**실행 순서**
- 스킬 내 효과 실행 순서 = 키의 `at` 오름차순. 동일 `at`는 키 id 순 → effectIds 배열 순.
- 시뮬레이터는 실시간 대기 없이 이 순서대로 즉시 정산한다(결과 동일).
- ACTIVE 스킬에서 어느 키에도 바인딩되지 않은 효과가 있으면 로드 에러. "종료 시 몰아서 발동" 같은 안전망은 두지 않는다.
- PASSIVE 스킬은 타임라인 없이, 트리거 성립 순간 `effects` 배열 순서대로 즉시 정산한다. 패시브 연출은 반응형 이벤트 구독으로 처리한다.
- 다단 히트: 히트마다 별도 효과를 두고 서로 다른 `at`에 바인딩(각 히트 독립 정산). 현재 스킬 풀에는 없음.

## 11. 연출(VFX) 처리

연출은 시각/청각 표현이며 효과(로직)와 완전히 별개 레이어다.

**원칙**
- 연출은 전투 결과를 절대 바꾸지 않는다. 결과는 효과 정산에서 이미 확정된다.
- 시뮬레이터에서는 연출을 렌더링하지 않는다(큐 생성 자체를 건너뛰거나 로그로만 기록).
- 연출은 배속 대상(재생 속도 스케일). 스킵은 없다.
- 로직은 연출을 기다리지 않는다. 턴 로직은 즉시 완결되고, UI 레이어가 연출 큐를 순차 재생한다. 다음 턴의 시각화는 현재 턴 큐 재생 완료 후 진행. 배속은 큐 소비 속도만 높이며 순서·결과 불변.
- 반응형 연출의 재생 순서·그룹핑은 연출 레이어의 재량이다. 이벤트는 seq 순으로 수신하되, 재생 큐 배치(동시 재생·묶음 재생 등)는 결과에 영향이 없으므로 자유롭게 조정할 수 있다.

**두 가지 발동 경로**

| 경로 | 방식 | 예 |
|---|---|---|
| 저작형 | 타임라인 키에 연출 큐 바인딩(스킬 고유 연출) | 시전 마법진, 발사 머즐, 검격 궤적 |
| 반응형 | 정산 결과 이벤트를 구독해 자동 재생(개별 저작 불필요) | 피격 스파크, 치명타 강조, 사망 소멸, 회복 광채 |

**VfxCue**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 큐 ID |
| asset | string | 파티클/사운드/애님 리소스 참조 |
| anchor | enum | CASTER, TARGET, GROUND, SCREEN |
| socket | string? | anchor 세부 위치(hand_r, chest 등) |
| loop | bool | true = 지속형(효과 수명 종속), false = 원샷 |
| channel | enum | VFX, SOUND, CAMERA — 사운드·화면 연출도 동일 큐로 통합 |

**지속 연출 수명** — loop 연출(버프 오라·디버프 표식)은 해당 지속 효과 인스턴스의 수명에 종속. 부여 시 스폰, 만료·해제 시 디스폰. STACK이면 스택 수, SEVERAL이면 인스턴스 수를 아이콘 뱃지로 노출.

**반응형 이벤트 매핑** — 매핑 엔트리 채우기는 콘텐츠 작업(구조만 확정).

| 이벤트 | 대표 연출 |
|---|---|
| HIT | 임팩트 스파크 + 히트 사운드 |
| CRIT | 치명 강조(전역 연출 포함) |
| DEATH | 소멸 연출 |
| HEAL | 회복 광채 |
| IMMUNITY_BLOCK | 방어 표식 |
| TAUNT_APPLIED | 도발자 강조 |
| REDUCTION_APPLIED | 실드 플래시 |

**발사체** — 회피·명중이 없으므로 발사체는 순수 연출이다. 로직 히트는 항상 DAMAGE 효과의 키 `at`에서 확정되고, 발사체는 확정된 결과를 도착 시점에 시각화할 뿐이다. 저작 규약: 발사체의 시각적 도착 순간에 히트 키를 배치한다. 배속 시 발사체 속도도 함께 스케일.

**전역 화면 연출** — 치명타·처치·보스 등장 등은 이벤트 스트림(CRIT, DEATH 등)을 구독하는 전역 연출로 처리. 히트스톱·카메라 셰이크 포함. 스킬 고유의 특수 화면 연출만 저작형.

## 12. 데이터 저작 & 관리

**대원칙 — 저작과 런타임 분리.** ScriptableObject/시트는 저작 전용이며, 런타임은 이를 엔진 비의존 순수 데이터(UnitDef/SkillDef/Effect)로 변환해 사용한다. 런타임 로직이 SO에 직접 의존하면 시뮬레이터가 Unity 없이 돌지 못한다.

**계층 구조** — 작업 단위 = 스킬 SO 하나. 작업자는 SkillSO 하나만 열면 그 스킬의 효과·타임라인·연출이 한 인스펙터에 모인다.

| 계층 | 단위 | 담는 것 | 형식 |
|---|---|---|---|
| 배포 | UnitSO | 기본 스탯·속성 방어·클래스·속성 + 스킬 참조 | SO 1 = 유닛 1 |
| 작업 | SkillSO | 효과·타임라인·타겟팅·쿨 등 스킬의 모든 조각 | SO 1 = 스킬 1 |
| 재사용 | EffectSO / VfxCueSO | 공용 효과·공용 연출 | SO(승격 시) |
| 밸런싱 | 시트 행 | 레벨별 스탯·계수·쿨·임계값 등 순수 수치 | 구글시트/CSV → 임포트 |

- **승격 기준:** 그 스킬 전용 1회성 조각은 SkillSO 안에 인라인 작성. 2개 이상 스킬이 공유하면 별도 SO로 승격·참조.
- **ID 자동 발급:** 효과·큐·키의 id는 수기 입력 금지, SO가 자동 발급. 타임라인의 effectIds는 드롭다운으로 선택(문자열 수기 금지) → 끊어진 참조 원천 차단.
- **시트 ↔ SO 분담:** 시트는 대량 수치·밸런싱 대상, SO는 구조·참조·에셋 링크. 밸런서는 시트만, 구조는 SO만 만져 편집 충돌 방지. 임포터가 시트를 SO/런타임 데이터로 변환.
- **인스펙터 검증:** OnValidate에서 검증 규칙(9절)을 실시간 검사 + 끊어진 참조 감지 + 타임라인 키 위치 미리보기.
- **카탈로그:** 효과 타입↔핸들러, 공용 VfxCue, 공용 상태 템플릿(도트/버프)을 한곳에서 조회. 신규 스킬은 카탈로그에서 골라 조립한다.

## 13. 스킬 데이터 예시

기존 샘플(아군 5 + 몬스터 4)의 스키마 환원. 전부 코드 분기 없이 표현된다. 몬스터 수치는 밸런싱 전 예시값.

| 스킬 | 구성 |
|---|---|
| 페리온 마검의 각성 (P) | Trigger: HP_THRESHOLD(self, ≤, 0.5) → STAT_MODIFIER(ATK +30%, REALTIME) + STAT_MODIFIER(SPD +10 고정, REALTIME), target = SELF |
| 자네트 유연한 자세 (P) | Trigger: POSITION(self, BACK) → STAT_MODIFIER(CRIT_RATE +20%, REALTIME) + STAT_MODIFIER(SPD +5, REALTIME), target = SELF |
| 자네트 헤드샷 (A) | targeting: ENEMY_LOWEST_HP → DAMAGE(2.2, guaranteedCrit), target = MAIN_TARGET |
| 에멜 저주의 낙인 (A) | targeting: ENEMY_SINGLE → DAMAGE(1.0, DARK) + DOT(0.4, DARK, 3턴, VICTIM_TURN, SEVERAL), target = MAIN_TARGET |
| 에멜 혼의 끌어당김 (A) | targeting: ENEMY_ALL → DAMAGE(0.8, [DARK, GRASS], lifestealRatio 0.3), target = MAIN_TARGET |
| 오를리아 치유의 기도 (A) | targeting: ALLY_INCL_SELF_SINGLE → HEAL(3.3) + IMMUNITY(1턴, VICTIM_TURN, category DARK), 둘 다 target = MAIN_TARGET(선택 1회 공유) |
| 오를리아 수호의 결계 (P) | Trigger: POSITION(self, FRONT) → DAMAGE_REDUCTION(0.15, target = ALLY_SAME_COLUMN, REALTIME) + STAT_MODIFIER(ELEMENT_DEF_LIGHT +20, target = ALLY_SAME_COLUMN, REALTIME) |
| 아델린 빙벽의 도발 (A) | targeting: ENEMY_ALL → TAUNT(2턴, VICTIM_TURN), target = MAIN_TARGET + STAT_MODIFIER(DEF +50%, 2턴, CASTER_TURN), target = SELF |
| 아델린 마검의 수호 (P) | Trigger: POSITION(self, FRONT) → DAMAGE_REDUCTION(0.30, target = ALLY_ADJACENT_LOWEST_HP, REALTIME) |
| 고블린 도적 기습 (A) | targeting: ENEMY_BACK_SINGLE, aiPolicy BACKLINE_FIRST → DAMAGE(1.2), target = MAIN_TARGET |
| 다크엘프 궁수 사격 (A) | targeting: ENEMY_LOWEST_HP → DAMAGE(1.1), target = MAIN_TARGET |
| 죽음의 사제 치유 (A) | targeting: ALLY_LOWEST_HP → HEAL(2.5), target = MAIN_TARGET |
| 그림자기사 어둠의 사슬 (A) | targeting: ENEMY_BACK_ALL → DAMAGE(1.3, DARK) + STAT_MODIFIER(SPD 30 down, 2턴, VICTIM_TURN), target = MAIN_TARGET |

## 14. 데이터 구성 예시 시나리오

스킬 하나가 데이터로 조립되어 전투에서 움직이기까지의 과정을 "에멜 — 저주의 낙인"으로 따라간다.

**1단계 — 스킬 껍데기(SkillDef)를 만든다**

| 필드 | 값 | 왜 이렇게 |
|---|---|---|
| slot / type | SKILL1 / ACTIVE | 자기 턴에 선택해서 쓰는 스킬 |
| elements | [DARK] | 어둠 속성 피해 — 대상의 어둠 속성 방어와 상호작용 |
| cooldown / initialCooldown | 3 / 0 | 전투 시작부터 사용 가능, 이후 자기 턴 3회 대기 |
| targeting.scope | ENEMY_SINGLE | 플레이어가 적 앞열에서 1명을 직접 고른다 |

**2단계 — 효과(Effect)를 조립한다**

| id | type | target | 내용 |
|---|---|---|---|
| fx_hit | DAMAGE | MAIN_TARGET | 즉발 피해. value 1.0(공격력 계수), element DARK |
| fx_curse | DOT | MAIN_TARGET | 지속 피해. value 0.4, DARK, 3턴(VICTIM_TURN), 중첩 SEVERAL |

두 효과 모두 MAIN_TARGET — 플레이어가 고른 한 명에게 피해와 낙인이 함께 들어간다. 중첩을 SEVERAL로 둔 이유: 낙인을 여러 번 걸면 각각 독립적으로 쌓이고 각자 만료되게 하기 위해서다.

**3단계 — 타임라인에 바인딩한다**

| 키 | at | 발동 효과 | 연출 의도 |
|---|---|---|---|
| k1 | 0.45 | fx_hit | 검이 대상에 닿는 프레임에 피해 확정 |
| k2 | 0.55 | fx_curse | 낙인 문양이 새겨지는 프레임에 도트 부여 |

애니메이터가 타격 프레임을 바꾸면 at 값만 옮기면 된다 — 피해량·지속 턴 같은 로직 데이터는 그대로다.

**4단계 — 검증을 통과하는지 확인한다**

적 대상 스킬에 MAIN_TARGET 피해 효과 ✓ · 모든 효과가 타임라인 키에 바인딩됨 ✓ · value 양수 ✓ · initialCooldown ≤ cooldown ✓ → 로드 성공.

**실행 시나리오 — 이 데이터가 전투에서 움직이는 순서**

1. 에멜의 턴. 잔여 쿨 0, 발동 조건 없음, 적 앞열 생존자 존재 → 스킬 버튼이 활성화된다.
2. 플레이어가 스킬을 선택 → 적 유효 앞열이 하이라이트 → 고블린 도적을 탭 = 주 대상 확정.
3. 재생 45% 지점(k1): fx_hit 정산 — 데미지 공식으로 피해 확정, HP 감소, 정산 후 처리.
4. 재생 55% 지점(k2): fx_curse 부여 — 같은 주 대상에게 낙인. 이미 낙인이 있어도 SEVERAL이라 별도 인스턴스로 쌓인다. 고블린이 이번 라운드 행동을 이미 마친 상태라면 "발동 대기"로 표시된다(DOT_PENDING).
5. 이후 고블린의 매 턴 종료마다 낙인이 1틱씩 발동 — 3턴 동안 정확히 3틱 후 소멸.

이 전 과정에 프로그래머 개입은 없다. 기획자가 SkillSO 인스펙터에서 조립하는 작업이 전부다.

---

# Phase 3 — 전투 상태 & 그리드

2×3 진형은 이 게임의 공간 언어다. 위치가 곧 역할이 되고(앞열 = 방패, 뒷열 = 화력·지원), 사망한 칸을 비워 두는 규칙 때문에 진형이 무너지는 것 자체가 전투의 리스크로 작동한다.

## 1. 그리드 좌표계

```
        col1   col2   col3
FRONT [ F1 ] [ F2 ] [ F3 ]
BACK  [ B1 ] [ B2 ] [ B3 ]
```

| 필드 | 타입 | 설명 |
|---|---|---|
| row | enum | FRONT, BACK |
| col | int | 1~3 |

진영별 독립 그리드. 빈 칸은 배치하지 않은 슬롯. 사망 시 해당 칸에서 유닛을 제거하며 빈 칸은 공석 유지(자동 이동 없음).

## 2. 전투 초기화

1. 편성(1~5명 선택)
2. 배치(각 유닛에 슬롯 지정, 마지막 설정 유지)
3. 전투 시작 → BattleUnit 인스턴스 생성(UnitDef 복제 + 런타임 상태 초기화)
4. initialCooldown 적용(챕터 진입 시 1회만 — 웨이브 전환 시에는 잔여 쿨 유지)
5. BATTLE_START 트리거 평가 → 1라운드 ROUND_START 트리거 평가
6. 행동 속도 정렬 → 라운드 1 진입

**편성 검증** — 전투 시작 전 강제. 위반 시 진입 차단.
- 인원 1~5명(0명 불가, 6명 이상 불가).
- 같은 캐릭터 중복 배치 금지(동일 UnitDef id 1회).
- 한 슬롯에 1유닛, 슬롯 범위 F1~F3/B1~B3.
- 배치는 연속일 필요 없음(빈 칸 허용). 적 덱도 동일 규칙으로 데이터 검증.

## 3. 런타임 유닛 상태

정적 UnitDef와 분리된 전투 중 가변 상태.

**BattleUnit**

| 필드 | 타입 | 설명 |
|---|---|---|
| ref | UnitDef | 원본 참조 |
| slot | Slot | 현재 슬롯 |
| currentHp | int | 현재 HP |
| alive | bool | 생존 여부 |
| cooldowns | 슬롯별 int | skill1·skill2 잔여 쿨(자기 턴 기준) |
| activeEffects | ActiveEffect[] | 현재 걸린 지속 효과 인스턴스 |
| actedThisRound | bool | 재정렬·중복 행동 방지 |

**ActiveEffect**

| 필드 | 타입 | 설명 |
|---|---|---|
| source | BattleUnit | 시전자(소멸 판정·계수 산출용) |
| def | Effect | 원본 효과 정의 |
| remaining | int? | 잔여 지속(basis에 따라 감소) |
| instanceId | string | 인스턴스 식별(SEVERAL 독립 누적 / STACK 카운터 관리) |

effective 스탯 = base + activeEffects의 STAT_MODIFIER 합산(계산 그룹 규칙). 매 참조 시 실시간 계산하거나 부여/해제 시점에 캐싱한다. REALTIME 패시브 때문에 effective 스탯은 항상 최신을 보장해야 한다(특히 spd — 재정렬 트리거).

## 4. 앞열/뒷열 판정

타겟팅에 쓰이는 것은 **유효 앞열** 개념이다.
- 유효 앞열 = FRONT 행의 생존 유닛 집합.
- 유효 앞열이 공집합(앞열 전멸)이면 앞열 대상 공격은 BACK 행으로 전환(Phase 5).
- 판정은 매 타격 시점에 재평가한다(공격 도중 앞열이 죽을 수 있으므로).

## 5. 인접 쿼리

보호 효과의 위치 규칙이 전부 여기에 의존한다. 순수 함수로 구현.

| 함수 | 반환 | 예(F2 기준) |
|---|---|---|
| sameColumn(slot) | 같은 col의 두 칸 | {F2, B2} |
| sideAdjacent(slot) | 같은 row 좌우 1칸 | {F1, F3} |
| oppositeRow(slot) | 같은 col 반대 row 1칸 | {B2} |

- ALLY_SAME_COLUMN 스코프 = sameColumn(시전자 슬롯)의 생존 아군(본인 포함).
- ALLY_ADJACENT_LOWEST_HP 스코프 = (sideAdjacent ∪ oppositeRow)(시전자 슬롯)의 생존 아군 중 HP 비율 최저 1명. 동률이면 앞열 우선 → col 낮은 순 → ID 순.
- 예) 아델린 F2 → 후보 {F1, F3, B2} → 그중 HP 비율이 가장 낮은 1명.

---

# Phase 4 — 턴 엔진

턴 순서는 이 게임의 시간 자원이다. 속도 버프/디버프로 행동 순서를 조작하는 것이 핵심 전략 축이며, "라운드당 전원 정확히 1회 행동" 불변식이 그 조작의 공정성을 보장한다.

## 1. 행동 속도 정렬 & 동률 처리

정렬 키(위에서부터 우선):

1. effective spd 내림차순
2. 아군 우선(ALLY > ENEMY)
3. 레벨 높은 순
4. 전투력 높은 순 — base 스탯 기준 산출(버프 미반영, 전투 중 불변)
5. 유닛 ID 순(최종 결정자, 항상 유일)

완전 결정론적. 동률로 인한 비결정성 없음.

## 2. 턴 큐 · 사이클 · 라운드

- 1턴 = 유닛 1명의 행동.
- 1사이클 = 참여 유닛 전원이 각 1회 행동(최고→최저 spd 1바퀴).
- 1라운드 = 1사이클.

라운드 루프:

1. 모든 생존 유닛 actedThisRound = false.
2. ROUND_START 트리거 평가.
3. 미행동 큐 = 생존 유닛 전원을 정렬 규칙으로 정렬.
4. 미행동 큐에 유닛이 남아 있는 동안 반복:
   1. 맨 앞 유닛을 꺼낸다. 대기 중 사망했으면 건너뛴다.
   2. 턴 실행: 행동 가능 확인 → 스킬 선택(Phase 6) → 타겟 확정(Phase 5) → 효과 정산(Phase 7) → 턴 종료 처리.
   3. actedThisRound = true.
   4. 진영 전멸 체크(Phase 10) → 종료 시 즉시 탈출.
5. 라운드 종료 처리(아래) → 다음 라운드.

**불변식:** 재정렬이 몇 번 일어나든 모든 생존 유닛은 라운드당 정확히 1회 행동한다. 재정렬은 순서만 바꾸지 횟수를 바꾸지 않는다.

## 3. 행동 속도 즉시 재정렬

어떤 효과든 유닛의 effective spd를 변경한 그 즉시 재정렬한다. 데미지·회복·버프·디버프·패시브 진입/해제 등 spd에 영향을 주는 모든 처리 직후 발동(spd 무관 처리는 재정렬하지 않음 — dirty 플래그 권장).

- 대상 = 미행동 큐(actedThisRound = false)에 남은 유닛만.
- 이미 행동한 유닛과 현재 행동 중인 유닛은 제외. 현재 사이클에 재진입시키지 않는다.
- 재정렬은 미행동 큐를 정렬 키로 다시 정렬하는 것으로 끝. 다음 차례부터 새 순서 반영.

엣지 케이스:
- 현재 행동 유닛이 자기 스킬로 자기 spd를 바꾼 경우: 이번 사이클 행동은 이미 소비 → 변경은 다음 라운드부터 반영.
- 현재 행동 유닛이 타 유닛의 spd를 바꾼 경우: 그 유닛이 미행동이면 즉시 순서 이동. 예) 어둠의 사슬(spd 감소)이 아직 행동하지 않은 아군을 맞히면 그 아군은 즉시 뒤로 밀린다.
- 패시브 실시간 해제로 spd가 원복된 경우도 동일 트리거(HP 회복으로 각성이 꺼지면 spd 원복 → 재정렬).

## 4. 행동 불가 처리

턴 진입 시 행동 불가 상태이상이면 턴 스킵(스킬·타겟 없이 턴 종료 처리로 직행). actedThisRound는 true(라운드 소비됨).

- 스킵 턴에도 턴 종료 처리는 전부 진행한다 — 도트 틱을 받고, 지속 감소·쿨 감소도 정상 진행.
- 스킵 턴의 쿨 감소: 사용한 스킬이 없으므로 보유 액티브 전부 −1.
- 행동 불가를 유발하는 효과(기절 등)는 현재 스킬 풀에 없음. 효과 타입 확장 시 추가.

## 5. 턴 종료 · 라운드 종료 처리 순서

**턴 종료 처리**(해당 유닛 행동 직후, 정산 후 처리 완료 뒤):

1. 도트 틱 — 본인에게 걸린 도트 발동(Phase 7). 부여 라운드 예외는 Phase 7 도트 절 참조.
2. VICTIM_TURN 지속 −1 — 본인에게 걸린 해당 기준 효과 잔여 감소, 0이면 만료.
3. CASTER_TURN 지속 −1 — 본인이 시전자인 해당 기준 효과 잔여 감소.
4. 쿨타임 감소 — 본인의 다른 액티브 쿨 −1(Phase 6).
5. TURN_END 이벤트.

**라운드 종료 처리**(전원 행동 후):

1. ROUND 기준 지속 효과 잔여 −1.
2. ROUND_END → 다음 라운드 ROUND_START → 행동 속도 정렬.

순서 고정 이유: 도트 틱을 지속 감소보다 먼저 해야 "3턴 도트 = 정확히 3틱"이 성립한다(감소가 먼저면 마지막 틱 소실).

---

# Phase 5 — 타겟팅

"누구를 때릴 것인가"가 수동 전투의 매 턴 의사결정이다. 앞열 우선 규칙이 진형의 가치를 만들고, 도발과 저격이 그 규칙을 흔들며 심리전을 만든다. 타겟팅은 독립 모듈로 분리해 "어떤 슬롯들이 대상인가"만 반환하게 하고, 실제 효과 적용(Phase 7)과 분리한다.

## 1. TargetScope

| 분류 | 값 | 설명 |
|---|---|---|
| 참조 | MAIN_TARGET | 스킬의 주 대상을 그대로 참조(효과 전용, targeting.scope에는 사용 불가) |
| 적 | ENEMY_SINGLE | 적 단일(기본 앞열 규칙 적용) |
| 적 | ENEMY_BACK_SINGLE | 적 뒷열 단일 명시 |
| 적 | ENEMY_ALL | 적 전체 |
| 적 | ENEMY_FRONT_ALL | 적 앞열 전체 |
| 적 | ENEMY_BACK_ALL | 적 뒷열 전체 |
| 적 | ENEMY_LOWEST_HP | 적 중 HP 비율 최저 1 |
| 아군 | ALLY_SINGLE | 아군 단일(시전자 제외) |
| 아군 | ALLY_LOWEST_HP | 아군 HP 비율 최저 1(시전자 제외) |
| 아군 | ALLY_ALL | 아군 전체(시전자 제외) |
| 아군 | ALLY_INCL_SELF_SINGLE | 자신 포함 아군 단일 |
| 아군 | ALLY_INCL_SELF_LOWEST_HP | 자신 포함 아군 HP 비율 최저 1 |
| 아군 | ALLY_INCL_SELF_ALL | 자신 포함 아군 전체 |
| 아군 | SELF | 자신만 |
| 위치 | ALLY_SAME_COLUMN | 시전자와 같은 열의 생존 아군(본인 포함) |
| 위치 | ALLY_ADJACENT_LOWEST_HP | 시전자 인접(좌우 + 앞뒤) 생존 아군 중 HP 비율 최저 1 |

HP 기준 선정은 전부 **HP 비율(현재/최대) 최저**다.

## 2. 주 대상 선정

타겟 선택은 스킬당 1회. 흐름:

1. `targeting.scope`로 후보 집합 산출(아래 규칙들 적용).
2. 강제 규칙(도발)이 있으면 후보 집합이 이미 좁혀진다.
3. 입력 소스가 후보에서 1명 선택 — 아군은 플레이어가 UI로, 몬스터는 AI 정책(Phase 8)으로. 조건부 스코프(LOWEST_HP 등)는 시스템이 자동 확정.
4. 선택된 유닛 = 주 대상. `target = MAIN_TARGET`인 효과 전부가 이를 공유한다.
5. 고정 스코프 효과(SELF, ALLY_SAME_COLUMN 등)는 주 대상과 무관하게 효과 정산 시점에 독립 산정.
6. 광역 스킬은 선택 단계 생략 — 주 대상 = 범위 내 생존자 전체.

이 구조 덕에 플레이어와 AI가 타겟팅 로직을 공유하고, AI는 "후보에서 하나 고르는 규칙"만 얹으면 된다.

타겟 선택은 항상 1회·1명이다. "아군 2명 선택" 같은 복수 선택형 스킬은 이 스키마가 지원하지 않는다(주 대상 단일 참조가 구조의 근간). 해당 유형이 필요해지면 스키마 확장을 별도로 설계한다.

## 3. 기본 앞열 규칙 & 뒷열 전환

ENEMY_SINGLE의 기본 후보 = 상대 유효 앞열(Phase 3).
- 유효 앞열이 공집합이면 뒷열로 자동 전환.
- 광역(ENEMY_ALL 등)은 이 규칙과 무관하게 지정 범위의 생존자 전원.

## 4. 뒷열 명시 스킬

ENEMY_BACK_SINGLE / ENEMY_BACK_ALL은 앞열 생존 여부와 무관하게 뒷열을 직접 타겟한다.
- 뒷열도 공집합이면 폴백 없음 — 유효 대상 0이므로 그 스킬은 발동 불가(9절). 수동 전투이므로 플레이어가 다른 스킬을 선택한다.

## 5. 광역 범위

지정 범위의 생존 유닛 전원에 효과를 개별 적용. 대상 열거·정산 순서는 슬롯 순(F1→F2→F3→B1→B2→B3) 고정 — 치명 RNG 소비 순서가 여기 걸리므로 결정론에 필수. 흡혈은 전체 타격의 총 피해 합산 후 1회 회복(Phase 7). 열거 순서 산출은 교체 가능한 정렬 인터페이스로 분리해 구현한다(기본 구현 = 슬롯 순) — 다른 순서(HP 낮은 순 등)가 필요한 스킬이 생기면 결정론적 동률 처리 규칙과 함께 데이터 필드로 확장한다.

## 6. 도발 강제

도발은 **모든 단일 대상 공격 스킬**의 타겟을 도발자로 강제한다. ENEMY_SINGLE은 물론 ENEMY_BACK_SINGLE·ENEMY_LOWEST_HP 등 조건부·뒷열 명시 단일기도 스코프 제약을 무시하고 도발자를 타격한다. 광역·회복·버프에는 영향 없음. 조건부·저격기까지 이기게 한 것은 탱커의 역할 정체성을 세우기 위함이다 — 도발 한 번이 확실하게 한 턴의 어그로를 산다.

- 예외: `targeting.ignoresTaunt = true`인 스킬은 도발 강제를 받지 않는다. 저격·처형 컨셉 스킬에 데이터로 지정한다.
- 적용 시점: 타겟 확정 직전. 이 시점에 도발자 생존 여부를 실시간 재조회한다 — 턴 진입과 타겟 확정 사이에 도발자가 죽었을 수 있다.
- 다중 도발 충돌: 도발자가 2명 이상이면 가장 마지막에 걸린 도발(부여 이벤트 seq 최대)의 시전자를 강제 타겟.
- 도발자 사망 시 해당 도발 즉시 소멸, 남은 도발 중 가장 최근 것으로 위임. 위임할 도발이 없으면 원래 스코프의 후보 집합으로 복귀한다.

## 7. 조건부 선정

LOWEST_HP 계열은 선정 시점의 HP 비율 최저 대상을 고른다. "가장 약한 적" 같은 중의적 표현은 쓰지 않는다 — 항상 HP 비율 최저로 명시.
- 동률 처리: 앞열 우선 → col 낮은 순 → ID 순.

## 8. 플레이어 선택 vs AI 입력

타겟팅 모듈은 "가능한 후보 집합"을 반환하고, 최종 선택은 입력 소스가 한다.

| 진영 | 선택 주체 |
|---|---|
| 아군 | 플레이어가 매 턴 후보 집합에서 UI로 직접 선택 |
| 몬스터 | AI 정책이 후보 집합에서 선정(Phase 8) |

강제 규칙(도발)이 있으면 후보가 이미 강제되어 있고, 입력 소스는 그 안에서만 선택한다.

## 9. 타겟 없음 · 발동 불가 · 자기 타겟

- 유효 타겟 0인 스킬은 발동 불가 — 아군은 해당 스킬 버튼이 비활성화되고, 몬스터는 다음 우선순위 스킬로 넘어간다(Phase 6). 일반 공격조차 대상이 없으면 그 턴은 무행동으로 종료.
- 유효 타겟 판정은 "생존한 대상 존재" 여부만 본다. 예상 회복량은 보지 않는다 — 풀피 아군만 있어도 힐은 정상 발동하며 회복량 0으로 처리(초과 회복 버림). 발동 여부가 상태에 따라 흔들리지 않아 결정론·구현이 단순해진다.
- 광역기는 대상이 1명이라도 있으면 발동, 0이면 발동 불가.
- 자기 포함 여부는 스코프가 명시한다: ALLY_* = 시전자 제외, ALLY_INCL_SELF_* = 자신 포함, SELF = 자신만. 스킬 설명 텍스트도 이를 구분해 표기한다("아군 1명" vs "자신을 포함한 아군 1명").

---

# Phase 6 — 스킬 선택 & 쿨타임

아군의 스킬 선택은 온전히 플레이어의 몫이다 — 자동 전투가 없는 이 게임의 핵심 가치다. 쿨타임은 "언제 아낄 것인가"의 자원 관리 리듬을 만들고, 몬스터는 예측 가능한 규칙으로 움직여 플레이어가 대응을 설계할 수 있게 한다.

## 1. 발동 가능 판정

발동 가능 = 잔여 쿨 0 AND 발동 조건 충족 AND 유효 타겟 존재. 순수 함수로 구현해 아군 UI와 몬스터 AI가 공유한다.

- 잔여 쿨: `cooldowns[slot] == 0`.
- 발동 조건: trigger가 있으면 현재 상태로 평가(예: HP 조건).
- 유효 타겟: 0이면 발동 불가(Phase 5).
- 일반 공격은 쿨·조건이 없어 대상만 있으면 항상 발동 가능.
- 패시브 슬롯은 이 판정의 대상이 아니다(트리거로 별도 발동).

## 2. 스킬 선택 주체

수동 전투이므로 스킬을 고르는 주체가 진영별로 다르다. 발동 가능 판정(1절)은 공유하고, 고르는 방식만 다르다.

| 진영 | 선택 방식 |
|---|---|
| 아군 | 발동 가능 판정을 통과한 슬롯 집합을 UI에 노출(불가 슬롯은 비활성 표시), 플레이어가 그중 하나를 직접 선택 |
| 몬스터 | 스킬2 → 스킬1 → 일반 공격 순으로 평가해 발동 가능한 첫 스킬을 자동 사용. 존재하는 슬롯만 순회 |

전부 발동 불가면 일반 공격이 최종 폴백이며, 일반 공격조차 대상이 없으면 그 턴은 무행동으로 종료된다(Phase 5).

## 3. 쿨타임 부여·감소

쿨타임 카운트는 자기 턴 기준이며 한 곳(턴 종료 처리)에서만 처리한다.

- 사용 시: 사용한 액티브에 cooldown 값 부여.
- 자기 턴 종료 시: 본인 보유 액티브 중 **방금 사용한 스킬을 제외한** 나머지의 잔여 쿨 −1(0에서 멈춤). 방금 쓴 스킬까지 줄이면 실효 쿨이 1 줄어들기 때문. 스킵 턴은 사용 스킬이 없으므로 전부 −1.
- 쿨 N = "사용 후 자기 턴 N회 경과 시 재사용 가능". 예) 쿨 3 → 사용한 턴 제외, 이후 자기 턴 종료 3번을 거쳐 4번째 자기 턴에 재사용.

## 4. 초기 쿨타임

"초기 쿨 N" = 전투 시작(챕터 진입) 시 그 스킬의 잔여 쿨을 N으로 세팅. 웨이브 전환 시에는 재적용하지 않는다(잔여 쿨 유지, Phase 9).

- 초기 쿨 0 → 1라운드 자기 턴에 즉시 사용 가능.
- 초기 쿨 1 → 자기 턴 1회 경과 후 사용 가능.
- 검증: initialCooldown ≤ cooldown.

## 5. 패시브 트리거 발동 시점

패시브는 자기 턴 선택과 무관하게 트리거 시점에 평가된다.

| 트리거 | 평가 시점 |
|---|---|
| BATTLE_START | 전투 초기화(챕터 진입) 시 1회 |
| ROUND_START | 매 라운드 시작 |
| HP_THRESHOLD / POSITION (REALTIME) | 정산 후 처리의 패시브 재평가 단계(Phase 7) — 조건 진입 시 적용, 이탈 시 해제 |
| ON_ATTACK_LANDED / ON_CRIT_LANDED / ON_KILL | 정산 후 처리의 파생 트리거 단계(Phase 7) — 해당 정산의 명중·치명·처치 결과 기준 |

같은 트리거가 여러 유닛에서 동시 발동하면 행동 속도 정렬 순으로 평가한다(결정론 보장).

---

# Phase 7 — 효과 & 데미지 파이프라인

한 대의 피해가 만들어지는 서사다. 공격력과 방어의 힘겨루기 → 치명타의 한 방 → 속성 우위 → 탱커의 감쇄가 정해진 순서로 겹치며, 이 순서가 고정되어 있어야 플레이어가 피해량을 직관적으로 예측하고 역산할 수 있다.

## 1. 효과 정산 순서

### 단일 DAMAGE 효과

대상은 Phase 5에서 확정된 상태로 진입한다.

1. **기본 피해 산출** — 시전자 effective atk × 스킬 계수(value) vs 대상 effective def.
2. **치명타 판정** — 게임 내 유일 확률. guaranteedCrit이면 RNG 소비 없이 무조건 발동. 발동 시 치명 보정.
3. **속성 보정** — 스킬 속성 추가 배율 합 − 대상 속성 방어 합(속성 방어 디버프가 여기 반영).
4. **피해 감쇄(보호)** — 대상에 걸린 DAMAGE_REDUCTION들을 곱연산 적용. 위치 조건은 이 시점에 실시간 판정.
5. **반올림 & 최소값** — 반올림 후 최종 1회만 `max(1, 결과)` 하한 적용(단계별 내림 아님).
6. **흡혈 적립** — lifestealRatio가 있으면 확정 피해 × 비율을 적립(회복은 아직 미적용).
7. **HP 적용** — 대상 currentHp 감소, 0 클램프.

곱연산 체인: `기본 → ×치명 보정 → ×속성 보정 → ×감쇄 곱 → 반올림 → max(1, ·)`

### 광역 처리

대상마다 1~5를 개별 수행(위치·감쇄·치명을 각자 판정, 슬롯 순 고정) → 6에서 흡혈을 전 대상 합산 → 7 HP 일괄 적용.

정산 후 처리(아래)는 광역 효과 전체에 대해 1회 수행한다. 단, 파생 트리거(ON_ATTACK_LANDED 계열)는 명중한 대상 수만큼 슬롯 순으로 개별 발동한다.

### 정산 후 처리 (효과 하나 완료 시점)

순서 고정. 이 5단계는 타임라인상 다음 효과로 넘어가기 전에 완결된다. 따라서 앞 효과가 만든 사망·버프가 뒤 효과에 반영된다.

1. **사망 판정** — currentHp ≤ 0 → alive = false, 그리드 제거. 동시 사망: 같은 효과의 대상들은 타격 시작 시점 스냅샷(생존·위치) 기준으로 1~5를 마친 뒤 일괄 사망 처리 → 보호 주체와 피보호가 같은 타격에 죽어도 그 타격에는 보호가 적용된다. "타격"의 단위는 효과 1회 정산이다 — 다단 히트는 히트마다 별도 효과로 독립 정산되므로, 앞 히트에서 보호 주체가 죽으면 뒤 히트에는 보호가 적용되지 않는다.
2. **흡혈 회복 반영** — 초과 회복 버림. 이 시점에 시전자가 사망 상태면 회복 무효(적립분 소멸).
3. **REALTIME 패시브 재평가** — HP 임계·위치 조건의 진입/이탈을 적용한다. 재평가는 한 번만 수행한다. 이 재평가로 생긴 변화가 다른 REALTIME 조건을 다시 흔들더라도 곧바로 반복 평가하지 않고, 다음 효과를 정산할 때 자연스럽게 반영된다.
4. **파생 트리거** — ON_ATTACK_LANDED·ON_CRIT_LANDED·ON_KILL 발동. 광역은 명중 대상 수만큼 슬롯 순 개별 발동.
5. **spd 변동 시 즉시 재정렬**(Phase 4).

### HEAL / 비피해 효과

HEAL은 피해 계산 단계(1~4)를 건너뛴다 — 회복량 산출(시전자 atk × 계수, 치명 없음) → 적용(초과 회복 버림) → 이후 정산 후 처리 중 패시브 재평가·파생 트리거·재정렬(3~5단계)만 수행한다. STAT_MODIFIER·TAUNT·IMMUNITY 등 피해가 없는 효과도 부여 즉시 같은 3~5단계만 수행한다.

## 2. 데미지 공식

구조·순서·항 구성은 확정. 수치 계수는 임시값이며 밸런싱 단계에서 조정한다(임시식으로 시뮬레이션·구현 진행 가능).

```
방어 경감 = 대상.def / (대상.def + k_def)          # 0~1. k_def = 300 (임시 상수)
기본     = 시전자.atk × 스킬 계수 × (1 − 방어 경감)

치명 보정 = 발동 시 (1 + 시전자.critDmg − 대상.critRes), 비발동 시 1
속성 보정 = 1 + Σ(스킬 속성 추가 배율 − 대상.elementDef[속성])   # 하한 0
감쇄 배율 = Π(1 − 각 DAMAGE_REDUCTION 값)                        # 곱연산

최종 = max(1, ROUND(기본 × 치명 보정 × 속성 보정 × 감쇄 배율))
```

- k_def가 클수록 방어 효율이 낮아진다. 감산율 방식은 방어가 아무리 커져도 피해가 0으로 수렴하지 않아 밸런싱이 안정적이다.
- 속성 추가 배율 기본 1속성 +0.3 / 2속성 각 +0.3. 2속성이면 속성별 (추가 배율 − elementDef)를 각각 계산해 합산.
- 밸런싱 조정 대상: k_def, 계수 스케일. 순서·항 구성은 불변.
- 소수점 반올림은 일반적인 반올림(0.5는 올림)으로 통일한다. 게임 클라이언트와 시뮬레이터는 소수 계산에 쓰는 자료형과 곱셈을 적용하는 순서까지 똑같이 맞춘다 — 계산 방식이 조금이라도 다르면 같은 전투를 다시 계산했을 때 결과가 어긋나기 때문이다.

## 3. 치명타

게임 내 유일한 확률 판정. RNG 스트림에서 대상별 1회 뽑는다.

- 발동: `rng.next() < 시전자.critRate`. guaranteedCrit이면 RNG 소비 없이 무조건 발동.
- critRes는 확률이 아니라 치명 피해량 상쇄(치명 확률은 낮출 수 없음).
- 이벤트: 발동 시 CRIT, 아니면 HIT.

## 4. 속성 보정

상성 없음. 유불리는 "추가 배율 − 속성 방어"로만 발생.

- 속성 이점 = 속성 방어 디버프. STAT_MODIFIER(ELEMENT_DEF_X, down)를 부여하면 그 순간부터 X속성 피해가 증가한다. 별도 규칙 아님.
- elementDef가 추가 배율보다 크면 속성 보정 < 1(피해 감소) 가능. 최종 max(1) 하한은 유지.
- 무속성 스킬은 속성 보정 = 1(속성 방어 미적용).

## 5. 회복

- 회복량 = 시전자 atk × 계수. **치명 없음**(RNG 미소비) — 확률 판정은 피해 치명 하나로 국한.
- 초과 회복 버림: `currentHp = min(최대 체력, currentHp + 회복량)`.
- 흡혈: DAMAGE의 lifestealRatio. 확정 피해 × 비율을 적립 후 정산 후 처리에서 시전자에게 회복. 광역은 전 대상 총 피해 합산 후 1회. 회복 시점에 시전자가 사망 상태면 무효.
- 사망자에게는 회복 불가(부활 없음). 이벤트: HEAL.

## 6. 버프/디버프 적용 & 지속

STAT_MODIFIER 부여 시 중첩 규칙으로 인스턴스 수 확정 → 계산 그룹으로 값 합산.

- 지속 카운트: durationBasis 기준. VICTIM_TURN은 대상의 턴 종료 처리에서 −1, CASTER_TURN은 시전자의 턴 종료 처리에서 −1, 0 도달 시 만료(STAT_MODIFIER_EXPIRED).
- effective 스탯은 부여/만료 즉시 갱신, spd 변경 시 재정렬.

## 7. 도트

지속 피해. 기본 중첩 규칙 = SEVERAL(시전자·스킬 무관 각 인스턴스 독립 누적·독립 만료). 스킬이 명시하면 STACK 등으로 변경 가능.

- **틱 시점:** 대상의 턴 종료 처리에서 발동(1회 = 1틱) 후 remaining −1. DOT_TICK 이벤트.
- **틱과 감소는 항상 쌍이다** — remaining 감소는 틱 발동과 함께만 일어난다. "N턴 도트 = 정확히 N틱" 불변식.
- **부여 라운드 예외:** 부여 시점에 대상이 이미 이번 라운드 행동을 마친 경우, 그 라운드 내 대상의 첫 피격 시점에 1회 즉시 발동하고 그 자리에서 remaining −1(해당 라운드 턴 종료 틱은 이미 지났으므로 중복 없음). 그 라운드에 피격이 없으면 틱도 감소도 없음 — 다음 라운드 턴 종료부터 정상 진행(틱 소실 없음, 만료만 1라운드 지연). 이 예외 판정 시 DOT_PENDING 이벤트를 방출한다 — 로그·UI가 "부여됨"과 "발동 대기"를 구분 표기하기 위한 것으로, 첫 DOT_TICK 발생이 곧 대기 해제라 별도 해제 이벤트는 없다.
- **틱 피해 산출:** DAMAGE 파이프라인 재사용하되 치명 판정 없음(RNG 미소비). 속성 보정·감쇄는 적용.
- 도트는 해로운 효과 → 면역 차단 대상. 시전자가 사망해도 유지.

## 8. 면역

Phase 2의 면역 카테고리 규칙을 따른다. 요약: category 일치하는 해로운 지속 효과의 신규 부여 차단(IMMUNITY_BLOCK) + 기부여 동종 도트의 틱 차단(잔여 감소는 진행). 즉발 피해·회복은 무관. 지속은 durationBasis(주로 VICTIM_TURN) 기준.

## 9. 보호

보호 = DAMAGE_REDUCTION(피해 감쇄) + TAUNT(타겟 강제) 두 타입.

- **감쇄:** 위치 조건을 매 피격 실시간 판정, 대상에 걸린 감쇄들을 곱연산. 보호 주체 사망 시 즉시 소멸(동시 사망은 스냅샷 적용). 예) 15% + 30% 겹침 → 1 − (0.85 × 0.70) = 40.5% 감소.
- **도발:** 모든 단일 대상 공격의 타겟을 도발자로 강제(Phase 5). 광역·회복·버프 무관. 다중 도발은 최신 우선.
- 중첩: 둘 다 그룹 키에 시전자 포함 + OVERRIDE(Phase 2) — 시전자가 다르면 공존, 같은 시전자 재시전은 갱신.
- REDUCTION_APPLIED 이벤트: 감쇄가 적용된 피격 1회당 1회 방출. value = 감쇄 전 피해 − 감쇄 후 피해(흡수량), source = 보호 시전자.

## 10. HP 트리거 패시브

HP_THRESHOLD 트리거는 REALTIME — 조건 진입 즉시 적용, 이탈 즉시 해제. 평가는 정산 후 처리의 패시브 재평가 단계.

- 자기 HP 기반 패시브(자신 ≤ 50% 시 각성)와 적 HP 기반 타겟 선정(HP 비율 최저 적 지정)을 구분 — 전자는 패시브 on/off, 후자는 타겟팅.
- HP가 경계를 오르내리면 매번 적용/해제 + spd 변동 시 재정렬. 이벤트: STAT_MODIFIER_ADDED / EXPIRED.

## 11. 동시 사망

정산 후 처리에서 확정. 요약: 타격 시작 시점 스냅샷 → 개별 피해 확정 → 일괄 사망 처리 → 흡혈 회복 반영 → REALTIME 재평가 → 파생 트리거 → spd 재정렬. 보호 주체와 피보호가 같은 타격에 죽어도 그 타격에는 보호가 적용된다. "같은 타격" = 같은 효과 1회 정산 — 히트가 나뉜 다단 히트에는 각 히트 사이의 사망이 반영된다.

---

# Phase 8 — 몬스터 AI

몬스터는 똑똑함보다 "읽을 수 있는 위협"을 지향한다. 정책 기반 행동으로 패턴을 학습하고 대응하는 재미를 만들고, HP·쿨 비공개로 추론의 여지를 남긴다. 구조상으로는 Phase 5 후보 집합과 Phase 6 스킬 선택 위에 몬스터 전용 "고르는 규칙"만 얹으며, 나머지 파이프라인은 플레이어와 공유한다.

## 1. 스킬 선택

몬스터는 스킬2 → 스킬1 → 일반 우선순위 자동 선택(Phase 6)과 발동 가능 판정·쿨을 그대로 따른다. AI는 스킬 선택을 새로 만들지 않고 타겟 선정만 자체 규칙으로 얹는다.

## 2. 타겟 선정 정책

TargetScope가 후보 집합을 좁히고, 그 안에서 아래 정책으로 최종 대상을 고른다. 도발 강제가 최우선. 스킬별 `targeting.aiPolicy`로 오버라이드하며 미지정 시 THREAT_WEIGHTED.

| 정책 | 규칙 | RNG 소비 |
|---|---|---|
| THREAT_WEIGHTED (기본) | 후보 i의 가중치 = 선정 시점의 effective atk. 선택 확률 = 가중치 / 가중치 합 | 1회 |
| RANDOM | 후보 균등 추첨 | 1회 |
| LOWEST_HP | 후보 중 HP 비율 최저(동률 처리는 Phase 5) | 0회 |
| BACKLINE_FIRST | 후보에 뒷열이 있으면 뒷열만 남김 → 남은 후보에 THREAT_WEIGHTED | 1회 |
| FIXED_ROLE | meta.role(Class) 지정. 후보에 해당 클래스가 있으면 그들만 남김 → THREAT_WEIGHTED. 없으면 전체 후보로 폴백 | 1회 |

- RNG 호출은 타겟 확정 단계에서 정확히 1회. 후보 열거는 슬롯 순 고정(Phase 1). 결정론적 선정(후보 1명, LOWEST_HP, 도발 강제)은 RNG를 소비하지 않는다.
- 조건부 스코프(LOWEST_HP 등)·광역은 스코프가 이미 결정 → 정책 미개입.
- 예) 고블린 도적 기습 = BACKLINE_FIRST(뒷열 우선 타격 컨셉).

## 3. 정보 비공개

플레이어에게 공개하는 몬스터 정보 = 스킬 이름·효과 텍스트만. 비공개: HP 수치·현재 쿨·다음 스킬·행동 순서 위치.

- UI는 몬스터 HP를 비율 바로만 표시하거나 숨긴다. 내부 상태는 이벤트 스트림에 존재하되 렌더러가 마스킹.
- AI 결정은 RNG 기반이라 내부적으로 결정론적이지만 플레이어에게 비공개 → 추론 게임 성립.

---

# Phase 9 — 웨이브 & 챕터 진행

HP와 쿨타임을 이어가는 웨이브 연전이 자원 관리의 긴장을 만든다 — 이번 웨이브에서 낭비한 스킬은 다음 웨이브의 빚이 된다. 챕터 완료 시 전체 회복으로 그 긴장을 해소해 챕터 단위의 완결된 호흡을 만든다. 구조상으로는 단일 전투를 감싸는 바깥 루프다.

## 1. 전투 단위

전투 = 챕터 진입부터 클리어/패배까지의 연속 웨이브 전체.

| 항목 | 규칙 |
|---|---|
| RNG seed | 챕터 진입 시 1회 고정. 웨이브 전환에도 스트림 연속 |
| 이벤트 seq | 전투 전체에서 연속. 웨이브 경계에 WAVE_START / WAVE_END 방출 |
| 라운드 카운터 | 웨이브마다 1로 리셋(라운드는 웨이브 내부 개념) |
| BATTLE_START 트리거 | 챕터 진입 시 1회만 |
| initialCooldown | 챕터 진입 시 1회만 적용(웨이브 전환 시 잔여 쿨 유지) |
| ROUND 기준 지속 효과 | 유지분은 remaining 그대로 이월, 새 웨이브의 라운드 종료마다 −1 |
| 패배 재시작 | 새 전투 = 새 seed 파생 |

## 2. 웨이브 전환

현재 웨이브 적 전멸 시 자동으로 다음 웨이브 시작(확인 버튼 없음).

| 항목 | 웨이브 간 처리 |
|---|---|
| 아군 HP | 유지(회복 없음) |
| 사망 캐릭터 | 유지(부활 없음) |
| 스킬 쿨타임 | 유지(초기화 없음) |
| 편성·배치 | 변경 불가 |
| 지속 효과 | 시전자 진영 기준 — 시전자가 ENEMY인 지속 효과는 전부 제거, ALLY인 지속 효과는 전부 유지(효과의 유불리 무관) |

각 웨이브는 독립 적 덱(종류·수·배치). 웨이브 수는 챕터별 개별 설정. 몬스터는 웨이브 경계를 넘어 유지되지 않는다(이전 웨이브 개체의 HP·상태 승계 없음).

## 3. 챕터 완료

마지막 웨이브 클리어 시 챕터 완료. HP·쿨타임 전부 회복, 사망 캐릭터 복귀 — 챕터 단위 독립. 다음 챕터는 풀 상태로 시작.

## 4. 패배 처리

페널티 없음. 1웨이브부터 재시작, 편성 화면 재노출(마지막 설정 유지). 클리어 전까지 스토리 미진행.

## 5. 데이터 구조

24챕터 × 챕터당 1~N웨이브. 회차 반복 재플레이 가능.

**ChapterDef**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 챕터 ID |
| waves | WaveDef[] | 웨이브 목록(길이 = 웨이브 수) |

**WaveDef**

| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 웨이브 ID |
| enemyDeck | {unitId, slot}[] | 적 유닛·배치 |

편성 UI는 전투 진입 전 매번 노출.

**데이터 구성 예시** — 1챕터를 2웨이브로 구성하는 경우:

| 데이터 | 값 |
|---|---|
| ChapterDef | id: chapter_01, waves: [wave_01a, wave_01b] |
| wave_01a.enemyDeck | 고블린 도적 → F1, 고블린 도적 → F3 |
| wave_01b.enemyDeck | 그림자기사 → F2, 다크엘프 궁수 → B2 |

웨이브 1의 적을 전멸시키면 확인 없이 웨이브 2가 시작되고, 아군의 HP·쿨타임·사망 상태는 그대로 이어진다.

---

# Phase 10 — 전투 종료 판정

승패가 확정되는 순간을 명확히 한다. 판정 시점이 흔들리면 "이겼는데 진" 억울함이 생기므로, 감지 타이밍과 동시 전멸 처리를 규칙으로 못 박는다.

## 1. 진영 전멸 감지

- 매 효과 정산 후(사망 판정 직후) 양 진영 생존 수 검사.
- 한쪽이 0이면: 현재 효과의 정산(정산 후 처리 포함)까지만 완결하고, 타임라인의 남은 효과는 실행하지 않고 즉시 루프 탈출.
- 양측 동시 전멸(광역기로 마지막 아군·적이 같은 타격에 사망): 무승부 → 패배와 동일 처리(재시작). 클리어로 처리하면 전멸한 아군으로 다음 웨이브를 맞게 되어 부조리하다.

## 2. 결과 처리

- 승리: 다음 웨이브가 있으면 웨이브 전환, 없으면 챕터 완료. BATTLE_END(win).
- 패배: 패배 처리. BATTLE_END(lose).
- 결과는 이벤트 스트림의 종결점 → 기여도 분석·리플레이 마감.

---

# Phase 11 — 시뮬레이터 / 밸런싱 툴

밸런싱을 감이 아니라 통계로 한다. 같은 조합을 수천 판 돌려 승률 곡선을 뽑는 것이 목적이며, 이 도구가 있어야 수치 조정의 반복 속도가 유지된다. 저작·런타임 분리, 연출 스킵, 시드 주입, 이벤트 스트림 위에서 동작하며, 구현 순서상 Phase 12(UI)보다 먼저 구현한다.

## 1. 헤드리스 전투 실행

Unity·렌더링 없이 순수 데이터만으로 전투 1판을 끝까지 정산.

- 입력: 아군 편성·배치, 적 덱(웨이브), 명시 seed.
- 연출 전면 스킵, 타임라인은 `at` 순서만 사용(실시간 대기 없음).
- 출력: 승패·최종 상태 + 이벤트 스트림.

## 2. 전투 로그 포맷

로그 = 이벤트 스트림의 직렬화. 별도 로그 로직 없음. 라운드/턴 경계, 각 효과 적용, 치명/사망/회복이 seq 순으로 재구성 → 리플레이·디버깅에 그대로 사용.

## 3. 기여도 분석

이벤트 스트림을 구독·집계해 산출. 별도 계산 훅 없음.

| 지표 | 집계 |
|---|---|
| 딜 | 시전자별 HIT/CRIT/DOT_TICK value 합 |
| 탱 | 시전자별 REDUCTION_APPLIED value(흡수량) 합 + 도발 유효 중 도발자가 받은 단일 공격 피해 합(강제 타겟 이벤트에 도발 효과 id를 마킹해 집계) |
| 힐 | 시전자별 HEAL value 합(흡혈 포함) |

## 4. 배치 반복 실행 & 통계

동일 조합을 seed만 바꿔 N회 반복 → 승률·평균 라운드·생존 수·기여도 분포.

- 분산의 원천은 치명 RNG와 AI 추첨뿐 → 밸런싱 민감도 측정에 적합.
- 계수·쿨·행동 속도를 바꿔가며 승률 곡선을 뽑는 것이 이 툴의 목적.

---

# Phase 12 — UI/UX

이 게임의 전투는 "보는" 전투다 — 배속은 허용하되 스킵은 없다. 매 턴의 선택과 연출이 곧 콘텐츠이기 때문이다. UI는 로직과 완전히 분리해 상태 렌더와 입력 전달만 하고 결과를 계산하지 않는다. 배속 지원 / 스킵 없음 / 자동 전투 없음.

## 1. 편성 UI

전투 진입 전 매번 노출. 1~5명 선택 + 2×3 그리드 배치. 마지막 설정 유지. 편성 검증(Phase 3) 위반 시 진입 차단.

## 2. 전투 화면

렌더 대상: 양 진영 그리드(빈 칸 포함), 현재 턴 순서(아군만 정확 표시, 적은 마스킹 가능), 아군 HP 수치, 적 HP는 비율 바 또는 숨김. 상태 효과 아이콘은 지속 효과 인스턴스 수명에 종속하며, STACK/SEVERAL 수를 뱃지로 표시.

## 3. 타겟 선택

아군 턴에 발동 가능한 스킬 버튼만 활성화(쿨·조건·타겟 부재 시 비활성 + 사유 표시) → 플레이어가 스킬 선택 → 후보 집합 하이라이트 → 탭으로 대상 선택. 도발 등 강제가 있으면 후보가 이미 1개로 좁혀져 표시. 광역기는 범위 프리뷰.

## 4. 스킬·연출 재생 & 배속

타임라인 + 연출 큐를 재생. 배속은 재생 속도만 스케일하며 정산 순서·결과 불변. 스킵 없음 → 큐는 반드시 재생하되 빨리 감기 가능.

## 5. 로그 / 결과 화면

전투 로그(이벤트 스트림)를 사람이 읽는 형태로 표시. 도트의 발동 대기 상태(DOT_PENDING)는 "대기 중"으로 구분 표기한다. 종료 시 승패 + 기여도 요약(딜/탱/힐).
